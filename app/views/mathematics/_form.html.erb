<%= form_for(mathematic) do |f| %>
  <% if mathematic.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(mathematic.errors.count, "error") %> prohibited this mathematic from being saved:</h2>

      <ul>
      <% mathematic.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %>
    <%= f.text_field :title %>
  </div>

  <div class="field">
    <%= f.label :message %>
    <%= f.text_field :message %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
